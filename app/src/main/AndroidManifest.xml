<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Re_Authentication"></activity>
        <activity android:name=".ServiceSettings" />
        <activity android:name=".RateService" />
        <activity android:name=".HiredServices" />
        <activity android:name=".PasswordRecovery" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".Menu" />
        <activity android:name=".Register" />
        <activity android:name=".Profile" />
        <activity android:name=".CategoryList" />
        <activity android:name=".Comments" />
        <activity android:name=".FullListShort" />
        <activity android:name=".MyServices" />
        <activity android:name=".Service" />
        <activity
            android:name=".Login"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>